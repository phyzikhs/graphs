package com.maths.graphs

import Main.default

/**
 * Graphs first plan:
 * Create type of graph:
 * Simple/unweighted: Connected, Disconnected, Complete,
 *                    Cycle, Cyclic, Bipartite, k-Partite,
 *                    Tree, Forest
 * Weighted
 * Directed
 * Directed Weighted
 * Print/Visualise Graph
 * */

object Main extends App {
  def default(a: String): Unit = {
    println(s"Hello the string is:\n$a")
  }

  default("Welcome to the introduction of this project.")
}
